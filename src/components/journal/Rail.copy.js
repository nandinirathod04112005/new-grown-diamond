/**
 * The rail's arrow labels, in the three languages (see src/i18n/useCopy.js).
 * `{label}` is the rail's own name, which the page passes in already
 * translated.
 */
export default {
  en: {
    previous: 'Previous — {label}',
    next: 'Next — {label}',
  },

  hi: {
    previous: 'पिछला — {label}',
    next: 'अगला — {label}',
  },

  gu: {
    previous: 'પાછલું — {label}',
    next: 'આગલું — {label}',
  },
};
